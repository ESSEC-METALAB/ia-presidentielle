# Environments and caches
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
build/
dist/
*.egg-info/

# Secrets. `.env.*` also catches .env.local and the like; the template is the exception.
.env
.env.*
!.env.example

# Collected content, database and reports: rebuilt by the pipeline, never committed.
/data/
