"""Composition root: the only module that instantiates concrete adapters and wires them.

TODO(step 5): commands collect, annotate, score, report, run-all and usage.
"""


def main() -> None:
    """Console entry point declared in pyproject.toml."""
    raise NotImplementedError("TODO(step 5): wire the adapters and expose the CLI commands.")
