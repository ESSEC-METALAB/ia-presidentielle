<!-- Prompt, version 1: classify a segment into the dimensions of config/taxonomy.yaml.
     TODO(step 4). A published prompt is never edited in place: a change is a new
     version file, so every annotation can name the exact prompt that produced it. -->
